# v0.0.40 — Delete published feedback

- Delete published human comments directly from their thread, with inline
  confirmation, cancellation, and retry after failure.
- Removing the last unseen comment releases the document from Feedback while
  preserving other comments, pending drafts, and independent Recent activity.
- Synchronize deletion across open clients and exclude deleted comments from
  inbox reads, including `--unseen`, thread views, exports, and read receipts.
- Persist deletion across server restarts and prevent stale reads or writes from
  restoring deleted feedback. Repeated deletion requests are safe to retry.

Upgrade the Vivi binary and restart running servers to use published comment
  deletion. An agent may already have fetched a comment before it is deleted.

## Verification

`task check` passed: 507 tests, 76 E2E cases, 100 Storybook image comparisons,
all Go tests, architecture checks, and UI/Go/Storybook builds. Storybook
interaction tests passed 98 cases with 2 existing skips. Cold viewer-loading
waits were adjusted without weakening assertions. Focused backend race tests
also passed.
